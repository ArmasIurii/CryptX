import { GrowDirectionDirective } from './grow-direction.directive';

describe('GrowDirectionDirective', () => {
  it('should create an instance', () => {
    const directive = new GrowDirectionDirective();
    expect(directive).toBeTruthy();
  });
});
